# notes

Working notes, investigations, scratch. The default home for anything not yet permanent —
promote to `spec/`, `decisions/`, or `reference/` once it stabilizes.

Name files `YYYY-MM-DD-short-slug.md`.
