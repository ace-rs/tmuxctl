# guides

Task-oriented how-tos for consumers of the crate — added as the public API stabilizes
(spawning a session, reading the event stream, sending keys, pushing a layout). Empty for
now; the crate is pre-implementation.
